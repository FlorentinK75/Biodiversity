extends Sprite2D

func _process(delta):
	# ??? Doesn't work, goal : orient picture into movement direction
	#var direction = $PathFollow2D.velocity.normalized()

	# guide the sprite in direction
	#look_at(direction) 
